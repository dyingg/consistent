name: Deploy API

on:
  push:
    branches: [main]
    paths:
      - "apps/core/**"
      - "packages/auth/**"
      - "packages/db/**"
      - "packages/contracts/**"
      - "packages/realtime/**"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # TODO: Add Fly.io or Railway deployment steps
      # - uses: superfly/flyctl-actions/setup-flyctl@master
      # - run: flyctl deploy --remote-only
      #   env:
      #     FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
